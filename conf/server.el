(server-start)

;;;; emacsclientで開いたものを閉じた時、ターミナルをアクティブにする
;;(add-hook 'server-done-hook
;;          (lambda ()
;;            (do-applescript "tell application \"Terminal\"
;;                                activate
;;                             end")))
